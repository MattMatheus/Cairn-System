---
title: Podman 5
description: Official Podman docs record for container runtime, compose behavior, and rootless/container operations.
doc_type: canonical_source
status: draft
tool: podman
vendor: Containers
version_policy: pinned_major
version: "5.x"
docs_base: https://docs.podman.io/en/latest/
allowed_domains:
  - docs.podman.io
entrypoints:
  - https://docs.podman.io/en/latest/
consult_when:
  - working with podman CLI behavior
  - container networking, volumes, rootless behavior, or compose semantics
  - checking runtime flags and container-device support
avoid_when:
  - the task is only about work harness process
notes:
  - If the host package manager pins an older Podman major, update this record or create a host-specific override.
---

# Podman 5

## Summary

Use the official Podman docs for runtime behavior, CLI flags, rootless constraints, and compose-adjacent semantics.

## References

- https://docs.podman.io/en/latest/
