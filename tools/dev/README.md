# Developer Tools

Local developer helpers for running, validating, and operating Cairn.

## Current Helpers

- `bootstrap_platform.sh`: prepares repo-local `.cairn/` runtime folders and validates the shared Go toolchain
- `build_release_artifacts.sh`: builds zipped memory-cli and tool-cli CLI artifacts for supported release targets
