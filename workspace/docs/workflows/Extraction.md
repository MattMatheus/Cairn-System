 This folder is the .codex directory which contains a lot of artifacts about my interactions with you the past few days.

  I'd like you to aggressively analyze the agent/human conversations to extract important details, decisions, techniques and anything else that might be valuable to a human or an agent.  When you've extracted this, prepare a new Extracted folder and then place data from Humans in the Humans folder and data gleaned from the Agent will go into Agent.
  
  The primary goal of this exercise is self improvement.  Both individually but also how we work as a team.

The corpus is pretty massive, use subagents to crawl.